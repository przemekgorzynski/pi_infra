---
- name: Collectd agent installation
  hosts: all
  gather_facts: yes
  user: przemek
  become: yes
  
  pre_tasks:
  - name: check connectivity
    ping:

  tasks:
  - import_role:
      name: collectd
      tasks_from: main.yml