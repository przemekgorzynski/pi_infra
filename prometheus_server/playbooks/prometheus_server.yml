---
- name: Initiall configuration
  hosts: all
  gather_facts: yes
  user: przemek
  become: yes
  
  pre_tasks:
  - name: check connectivity
    ping:

  tasks:
  - import_role:
      name: prometheus_server
      tasks_from: main.yml